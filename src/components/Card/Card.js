function Card(props) {
    return (
        <div class="projects">
            {/* <img class="project-img" src={""} alt={props.altTitle} />
            <a href={props.deployed}><h3>{props.title}</h3></a>
            <p>{props.description}</p>
            <a href={props.github} title="Code Repo"><h4>{props.projectTitle}</h4></a> */}
            <h1>hel;lo</h1>
        </div>
    )
}

export default Card